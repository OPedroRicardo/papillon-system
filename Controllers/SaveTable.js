/* export const saveTable = (arrayToSave) => {
    if(arrayToSave.length != 5) {
        throw new Error('Falta um item na lista do produto') // throw new Error('An item is missing from the product list')
    }
    localStorage.setItem('table', JSON.stringify(arrayToSave))
} */